name = "edge-gateway-admin"
main = "src/workers/admin.ts"
compatibility_date = "2026-08-17"
compatibility_flags = ["nodejs_compat"]

# Worker name, route, and runtime variables are injected from GitOps at deploy time.
