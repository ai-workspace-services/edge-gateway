name = "edge-gateway"
main = "src/index.ts"
compatibility_date = "2024-02-01"
compatibility_flags = ["nodejs_compat"]

# Runtime variables are injected from the rendered GitOps manifest at deploy time.
# JWT_SECRET is injected from Vault with `wrangler secret put`.
